import { AdminPlansClient } from "./_views/AdminPlansClient";

export const dynamic = "force-dynamic";
export const metadata = {
  title: "Plans · Admin",
};

export default function AdminPlansPage() {
  return <AdminPlansClient />;
}
