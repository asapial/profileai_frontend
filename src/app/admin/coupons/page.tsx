import { AdminCouponsClient } from "./_views/AdminCouponsClient";

export const dynamic = "force-dynamic";
export const metadata = {
  title: "Coupons · Admin",
};

export default function AdminCouponsPage() {
  return <AdminCouponsClient />;
}
